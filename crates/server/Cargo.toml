[package]
name = "jamhub-server"
version.workspace = true
edition.workspace = true

[dependencies]
jamhub-network = { path = "../network" }
tokio = { version = "1", features = ["full"] }
clap = { version = "4", features = ["derive"] }
